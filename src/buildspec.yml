version: 0.2

phases:
  install:
    commands:
      - echo Installing source NPM dependencies...
      - node -v
      - npm install
  pre_build:
  post_build:
    commands:
      - echo Build completed on `date`
artifacts:
  files:
    - '**/*'