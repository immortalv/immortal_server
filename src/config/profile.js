const PROFILE_TYPES = {
  PUBLIC: 'public',
  PRIVAT: 'privat',
};

const profilTypes = [PROFILE_TYPES.PUBLIC, PROFILE_TYPES.PRIVAT];
const profilTemplates = ['simple', 'book', 'article'];

module.exports = {
  PROFILE_TYPES,
  profilTypes,
  profilTemplates,
};
